export const version = '1.0.0';
// current environment # development, staging, test, symphonycard, production
export const environment = 'development';
// maintenance mode
export const maintenanceMode = false;